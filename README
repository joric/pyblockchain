Usage: pyblockchain.py [options]

Options:
  --version          show program's version number and exit
  -h, --help         show this help message and exit
  --block=BLOCK      dump block by index in json format
  --address=ADDRESS  get amount received by address
  --index            re-index blockchain
